var logic = {
        addProfile: function (object) {
            if (
            }

        }
        // when the user enter the information it needs to be stored as an object first.
        /* each field will be set to 
        object.userName = userName
        object.password = password
        object.age= age
        object.hobbies = hobbies 

        */
        //Where do I but these items

        // I will need to make an object prototype
        // https://github.com/jankeLearning/content-md/blob/master/programming-and-paradigms/03-oop-prototypes.md